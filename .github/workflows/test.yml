name: Test Apps
on:
  push:
    branches:
      - main
jobs:
  # TODO: replace with real per-app jobs once new packages are added.
  #  test:
  #    strategy:
  #      matrix:
  #        app:
  #          - <package-name>
  #    runs-on: macos-latest
  #    steps:
  #      - name: Install app
  #        run: brew install sv-tools/apps/${{ matrix.app }}
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "no apps configured yet"'
